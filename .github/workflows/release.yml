name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18.x'
        cache: 'yarn'
        registry-url: 'https://registry.npmjs.org'
        
    - name: Install dependencies
      run: yarn install --frozen-lockfile
      
    - name: Run tests (excluding performance)
      run: npm run test:ci
      
    - name: Run linter
      run: npm run lint
      
    - name: Build package
      run: npm run build
      
    - name: Publish to npm
      run: npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        
    - name: Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          ## 🚀 新版本发布

          ### 📋 更新内容
          请查看 [CHANGELOG.md](https://github.com/qiupo/sql-parser-js/blob/main/CHANGELOG.md) 了解详细更新内容。

          ### 📦 安装方式
          ```bash
          npm install sql-parser-ast-js@${{ github.ref_name }}
          ```

          ### 🔗 相关链接
          - [npm 包](https://www.npmjs.com/package/sql-parser-ast-js)
          - [API 文档](https://github.com/qiupo/sql-parser-js/blob/main/docs/API.md)
          - [使用示例](https://github.com/qiupo/sql-parser-js/blob/main/docs/examples.md)
        draft: false
        prerelease: false